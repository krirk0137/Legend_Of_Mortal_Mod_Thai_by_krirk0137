# --- Translation working backups (large; not needed by players) ---
*.EN-BACKUP
*.POST-*
*.PRE-*
*.gz

# --- Translation-project working files ---
_progress/
qwen_test/
_poc/          # josh StringTable-mod spike (kept locally to retry later; see memory)

# --- Claude / local session files ---
.claude/
scratchpad/

# --- BepInEx runtime-generated (regenerated on launch) ---
Mod/BepInEx/cache/
Mod/BepInEx/LogOutput.log

# --- OS junk ---
Thumbs.db
.DS_Store
desktop.ini

# Release artifacts (uploaded to GitHub Releases, not committed)
*.zip
