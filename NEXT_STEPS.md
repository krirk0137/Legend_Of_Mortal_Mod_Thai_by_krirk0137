# NEXT STEPS — Thai translation of Legend of Mortal

Handoff notes so a fresh session can continue with minimal context. See also `CLAUDE.md`
(pipeline/rules) and the memory files (project decisions).

---
# 🎯🎯 NEXT = v1.3 — PLAN (written 2026-07-14, user moving to a fresh session) 🎯🎯
v1.2 is RELEASED (tag `v1.2` pushed; zip at repo root; Story 01 + register + single-char fix +
Thai TMP font Kanit_sdf + resizer fix). Live file = current `Translation zh-CN to en.txt`.
Two jobs remain, in this order:

### ⭐ JOB 1 (BUG — user caught it) — GENDERED REGISTER: women are speaking like men

> **✅ STAGE A DONE (2026-07-16)** — deterministic, line-aligned, verified. Snapshot `.POST-V13-1A`,
> synced to live game. `tools/v13_job1a.py` (in scratchpad) using snapshot `.POST-STORY01v11` as the
> pre-sweep female-`ค่ะ` signal: **BUG 1a** 72 lines `ขอรับ`→`เจ้าค่ะ` (all female-ack lines);
> **BUG 1b** 102 lines `ข้าน้อย`→`ข้า` (101 female-pronoun keys 妾身/奴家/小女子 + 1 female-ack overlap).
> Verified: keys-vs-PRE-V13 diff = **0** (touched values only), `เจ้าค่ะ`=72, `ครับ`=0, bare `ค่ะ`=0,
> `ข้าน้อย` 887→785, female-pronoun lines still holding `ข้าน้อย` = 0. (Note: keydiff-vs-`.EN-BACKUP`=97
> is PRE-EXISTING v1.2 comment-outs, not this edit — verify against `.PRE-V13` instead.)
> **✅ STAGE B DONE (2026-07-16)** — `tools/v13_job1b.py`, snapshot `.POST-V13-1B`, synced. Data-driven:
> analyzed all 542 non-male-marked `ข้าน้อย` candidates → only DEFENSIBLE female signals were extended
> female SELF-reference terms in the key (`民女`×5, `妾`×5) + `上官萤` self-naming ×1 = **11 lines**
> `ข้าน้อย`→`ข้า`, EXCLUDING any line with a male marker (`在下`/`小人`/`小生`/`妻妾`) to kill false positives
> (`妻妾成群…男人的梦想`, `赵活…只属上官萤` = MALE mentioning a woman). **REJECTED signals:** `หนู`-in-value
> (it's 2nd-person `คุณหนู`, not self-ref) and female cast-names-mentioned-by-males. The remaining ~477
> `ข้าน้อย` have NO gender signal in the key → left as male (safe default; forcing them would inject errors).
> `ข้าน้อย` 887→774. **JOB 1 COMPLETE.** Next = JOB 2 (gap-fill).

The locked style treated register as gender-neutral. It is **not**. Two distinct male-only forms leaked
onto female characters:

| | MALE | FEMALE |
|---|---|---|
| acknowledgment particle | `ขอรับ` | **`เจ้าค่ะ`** (period-correct female form — NOT the banned modern bare `ค่ะ`) |
| humble self-reference | `ข้าน้อย` | **`ข้า`** (plain — women never say `ข้าน้อย`) |
| 2nd person / neutral | `เจ้า` / `ท่าน` / `ข้า` | same |

**BUG 1a — `ขอรับ` on female lines.** The v1.1 sweep blanket-converted `เจ้าค่ะ`→`ขอรับ` (44 lines) and
`ค่ะ`→`ขอรับ` (~28) because the style banned "ค่ะ" outright. Too blunt — `เจ้าค่ะ` is the correct FEMALE form.

**BUG 1b — `ข้าน้อย` on female lines.** Game-wide `ข้าน้อย` = **887** lines. Split by the CN source pronoun:
- key has `在下` (male humble) = 183 · `小人`/`小的` (male lowly) = 64 → **correct, leave alone**
- key has **`妾身`/`奴家`/`小女子` (unambiguously FEMALE) = 101** → **WRONG → change `ข้าน้อย` → `ข้า`**
- remaining ~539 have no gender marker in the key → need speaker/context detection (see signals below)

**Three signals to identify a FEMALE line (use all, cross-check):**
1. **CN source pronoun in the key** — `妾身` / `奴家` / `小女子` / `本宫` = female (101 hits for ข้าน้อย).
   `在下` / `小人` / `小的` = male.
2. **The original `ค่ะ` marker** — snapshot **`Translation zh-CN to en.txt.POST-STORY01v11`** is the state
   BEFORE the v1.1 register sweep and still holds each line's ORIGINAL `ครับ` / `ค่ะ`. Diff it vs the live file:
   originally-`ค่ะ`/`เจ้าค่ะ` ⇒ FEMALE; originally-`ครับ` ⇒ MALE.
3. **Speaker name** — Story dialogue blocks carry `<size\=28><color\=#D9B077>NAME</color></size>`.
   Known female cast (see `tools/glossary.tsv`): 唐默铃/โม่หลิง (小师妹), 云裳/หวินฉาง, 上官萤/ซ่างกวนอิง,
   龙湘/หลงเซียง, 小梅/เสี่ยวเหมย, 温夫人, 乐娘子, 郁竹, 萤儿/อิงเอ๋อร์.

**Then:** apply → `keydiff.pl` vs `.EN-BACKUP` = 0 → re-verify: game-wide `ครับ`=0 and bare-`ค่ะ`=0,
but **`เจ้าค่ะ` > 0 is now CORRECT and expected**. Also update the locked style in `CLAUDE.md` +
`tools/worker_prompt_*.txt` with the gendered table above so future workers don't repeat it.

### JOB 2 — translate the remaining gaps using `_AutoGeneratedTranslations.txt` as the worklist

> **📊 ANALYSED 2026-07-16 (user finished 1/54 endings; live `_auto` = 98 entries).** Categorised — do NOT
> bulk-paste; split into these 4 buckets (script the split, it re-runs as the file grows):
> * 🟢 **37 genuinely translatable (DO THESE)** — highest value = **story/event prose** currently rendered by
>   Google with the BANNED pronouns `คุณ`/`ฉัน` (`师父，你究竟要睡到何时才醒？…`, `小师妹惊觉你蹲在窗下…`,
>   `你心浮气躁…`, `你一个不留神，扭伤了脚…`, `你掌灯时分心打翻了灯油…`) + 4× achievement lines
>   (`达成风云史《…》` — Google's "สืบสานประวัติศาสตร์" is wrong, should be `บรรลุตำนาน《…》`) + item/skill names
>   (`纸甲`, `[天地无声势]`, `【乖乖水】`) + Traditional menu keys (`啟程`, `最近存檔`, `已開發`, `匯出成功`) +
>   event one-liners (`大师兄的绝招……`, `赌博胜负……`, `小师妹的决策……`) + the 【鞭炮】/【唐门小剑】/【脱手镖】
>   weapon-stat blocks. Also fix pinyin leaks in them (Yu Xiaomei/Tang Shan/Zhao Huo/Shentu Long → glossary Thai).
> * 🔴 **27 single-CJK-char keys — NEVER add** (`買 卖 一 二 三 四 上 下 左 右 我 你 门 唐 器 暗 …`). Same class
>   v1.2 commented out (97 entries). `你=คุณ` / `我=ฉัน` would be catastrophic (substring-match everywhere).
> * 💡 **Candidate shortcut for the number bucket:** `[Behaviour] TemplateAllNumberAway` (currently `False`).
>   It templates numbers away so `命中 91 %` / `命中 50 %` share ONE cached translation. NOT documented in the
>   bundled `README (AutoTranslator).md`, so **test it before trusting it** (watch for numbers being templated
>   inside story prose). If it works it replaces much of the regex.txt work below. One-line, reversible.
> * 🟠 **23 number/stat templates → `regex.txt` only** (`命中\r\nNN %`, `银两 -NNNN`, `近战伤害NN 爆击率N %`,
>   `骰子總數: …`, `解锁条件：银两>\=100`, `1.性情>\=60(目前62)`). Infinite variants; static keys can never match.
> * 🟡 **11 MIXED-LANG keys = a REAL BUG to fix at source** — our own Thai leaked INTO a Chinese key, then
>   Google filled the rest: `[เส้นทาง]回合數: 1…` and `ชิงเฉิง「逸尘道子」申屠龙…`. Proves the dict still has
>   standalone entries (`路径→เส้นทาง`, `青城→ชิงเฉิง`) that **partial-substitute inside longer strings** —
>   the v1.2 single-char bug class, but with MULTI-char terms. Find + comment out / lengthen those keys.


The user plays with `Endpoint=GoogleTranslateV2` ON **on purpose** (they read Thai/English but **NOT Chinese**,
so an English fallback is strictly better than leftover CJK — do NOT turn the endpoint off).
`_auto` therefore logs every gap: **its left-hand side is the EXACT source string the game emits** — that is
our missing key, verbatim (correct `\r\n`, correct Traditional form). Ask the user to play broadly, then send
the whole accumulated `_auto` file. Then:
1. Extract keys (left of `=`), dedupe, and split into three buckets:
   - **static** (e.g. `已開發`, `從來沒見過這種歹毒的武功…`) → translate to Thai → add to dict.
   - **dynamic** (numbers/dates: `新增日期：2024/01/01 12:59`, `平射伤害10  爆击率0 %`, `命中\r\n74 %`,
     `骰子總數: 4 \= 4`) → **must** be `regex.txt` rules (they can never match statically).
     These item/combat stat blocks are TEMPLATED — reuse the dedupe-template + expand approach already
     built for the UI-Changes block (`_progress/optional/ui_expand.pl`).
   - **Traditional (繁體)** keys (`匯出成功`, `唐門弟子的標準配備`, `骰子總數`, `玩家動作`) → the game emits
     Traditional in some UI even in Simplified mode; our dict is Simplified-keyed → add Traditional keys
     (OpenCC-convert the relevant Simplified keys, or just take them verbatim from `_auto`).
2. Also fix **Chinese left inside our own Thai values** — XUnity sees the CJK and re-sends the line to Google,
   which turns those bits into English. Known: the `正大光明` motto (line ~20206, Tang-Sect-meeting scene) and
   name+CJK item/bio strings (`点苍明珠`, `叶云舟的胞妹…` — those exist in the dict but with `\n`, while the game
   emits `\r\n`, so only the NAME token substitutes → `เตี่ยนชาง明珠` → Google → `เตี่ยนชางPearl`).
3. Apply → `keydiff.pl` vs `.EN-BACKUP` = 0 → in-game test → v1.3.

### 🎯 JOB 3 (NEXT SESSION — user chose this 2026-07-16) — SPEAKER-ATTRIBUTED register fix
**Problem the user hit:** female characters (e.g. 上官萤/ซ่างกวนอิง) still say the MALE `ขอรับ` / `ข้าน้อย`
in-game. JOB 1 only caught deterministic signals (snapshot-`ค่ะ` + female pronouns in the key = 186 lines);
most female dialogue was translated with `ขอรับ`/neutral from the start and has **no signal in the dict**.

**Why it can't be scripted from the dict alone (two hard blocks):**
1. The dict is a flat `key=value` store with **NO per-line speaker attribution** — the speaker name is a
   SEPARATE key (`上官萤=ซ่างกวนอิง`), never attached to the dialogue line. So "รู้จักขอรับ" carries no
   "who said it".
2. **`ขอรับ` is polysemous** — MALE sentence particle *vs* the verb "รับ" (`ขอรับคำสั่ง` = accept orders,
   `ขอรับใช้`). Blanket `ขอรับ→เจ้าค่ะ` breaks the verb sense. Must be context-aware per line.

**The fix (B — systematic):** extract the game's **Fungus flowchart / dialogue-with-speaker data** from the
asset bundles (AssetStudio / AssetRipper; or via the [[josh-stringtable-spike]] StringTable whose keys are
Fungus block IDs). Build a `speaker → gender` map (female cast in `tools/glossary.tsv`: 上官萤/ซ่างกวนอิง,
唐默铃/โม่หลิง, 云裳/หวินฉาง, 龙湘/หลงเซียง, 小梅/เสี่ยวเหมย, 温夫人, 乐娘子, 郁竹, 萤儿/อิงเอ๋อร์…). Then for each
female speaker's lines convert male→female register **context-aware** (particle `ขอรับ→เจ้าค่ะ`, `ข้าน้อย→ข้า`;
SKIP `ขอรับ`-as-verb). Interim stopgap = reactive per-line fixes (user screenshots/quotes the wrong lines).

**Already done 2026-07-16 (don't redo):** analyzed all `ขอรับ` lines with a female pronoun in the key = only 2
(`奴家遵命…` L49277 — that `ขอรับ` is the VERB, correctly left; `妾欲亲往…` L59301 — genuine particle, FIXED
→ `เจ้าค่ะ`, snapshot `.PRE-V13-1C`, synced). Confirmed no `上官萤` self-naming line still on `ขอรับ`.

### ❌ NOT translatable (don't chase it)
The **"ตำนาน" (Legend/codex) menu** — even the English base doesn't translate it, i.e. XUnity cannot SEE the
text: it is almost certainly a **texture/image**, not real text. Only texture replacement could do it
(a completely different pipeline). Out of scope.

### Key context for the next session
- Config (locked, do not "fix"): `Endpoint=GoogleTranslateV2` **ON** (English fallback is wanted),
  `MaxCharactersPerTranslation=2000`, `FallbackFontTextMeshPro=Kanit_sdf` (Thai TMP — fixes □ tofu;
  Fallback beats Override: it keeps the game's fancy fonts), `OverrideFont=Tahoma` (UGUI).
- Game: **Unity 2020.3.49f1 / TMP 1.4.0**. Font guide: `TMP_THAI_FONT_GUIDE.md`.
- 97 single-CJK-char dict entries are **commented out** (`//唐=ถัง` etc.) — they used to partial-substitute
  into composite strings (`三师兄`→`สาม师兄`) and produce Thai+Chinese+English soup. Leave them commented.
- Backups: `.EN-BACKUP` (anchor, line-aligned), `.POST-V11`, `.PRE-SINGLECHAR`, `.POST-STORY01v11` (← Job 1 needs this one).

---

# ✅✅ v1.1 DONE (2026-07-13) — Story 01 re-translated + game-wide register cleaned ✅✅
Both v1.1 QA problems are FIXED and applied to the live file. Current good state = snapshot **`.POST-V11`** (keydiff vs `.EN-BACKUP` = 0). **Only zip rebuild + optional git commit + in-game test remain.**

### ✅ PROBLEM 1 FIXED — Story 01 re-translated whole (was 773 English-name leaks)
Re-translated Story 01 (orig lines **18494–22173**, 3675 entries) from scratch via the mature worker pipeline. **Result: name-leak 773→0, register 0, term-slips fixed (江湖→ยุทธภพ, 暗器→อาวุธลับ).** keydiff=0. Snapshot `.POST-STORY01v11`.
- **Chunking lesson (NEW):** the 200-line chunk c01_02 (35 KB) **hit the 64k output-token ceiling** even though c01_01 (31 KB) passed — the pass/fail line for Story-01-density prose is ~33 KB source. Fix that worked: **re-split the remainder BY BYTES ≤27 KB/chunk** (not by line count — line count doesn't bound bytes; a 150-line chunk hit 38 KB). Did the whole chapter as c01_00,c01_01 (200-line, done before the ceiling hit) + **23 byte-capped chunks c01b_00..c01b_22**, 3–6/wave, worker prompt `tools/worker_prompt_story01.txt` (short launch prompt points to it). All 25 chunks PASS per-chunk verify (`_progress/story01/verify_chunk.sh <base>` — key-diff via extract_keys, latin-in-key is a WARNING not fail since `<size\=NN>` tags + literal `\n` + legit source-English keys like `Present` all false-positive it). Assembled `_progress/story01/story01_gold.tsv` = 3675, key-diff vs source = 0.
- **Applied RANGE-RESTRICTED (NOT global):** apply_thai.pl is global-by-key and Story-01 keys duplicate ~4× game-wide (好。→202 lines, 是。→144), so a global apply would clobber ~12k out-of-range lines. Instead: sliced the file into head(1–18493)+mid(18494–22173)+tail, applied gold to **mid only**, re-concatenated. head/tail byte-identical to before (verified). This is the SAFE way to re-apply one chapter into a finalized file.
- Glossary additions this pass (now **455**): 点苍=เตี่ยนชาง, 点苍派, 练功塔=หอฝึกวิทยายุทธ์, 南宫(standalone)=หนานกง, 彼岸仙香, 丐帮=พรรคขอทาน, 沧帮=พรรคชางปัง(match game-wide 39×), 白鲨帮=พรรคฉลามขาว, 石帮主=เจ้าพรรคสือ, 牛长寿=หนิวฉางโซ่ว.

### ✅ PROBLEM 2 FIXED — game-wide register cleanup (was ครับ214/ค่ะ90/เธอ128/หม่อมฉัน25)
After re-translating Story 01, **เธอ dropped 128→0 entirely** (all 128 were in the Story-01 pilot). Remaining ครับ/ค่ะ/หม่อมฉัน (many in Story02-17 — the old per-chapter QA reported 0 because of the perl-CSD false-0 gotcha; they were real) cleaned via staged byte-level replace on the whole file (values only; keys are Chinese so untouched, keydiff=0):
```
perl -i -pe 's/หม่อมฉัน/ข้า/g'      # pronoun
perl -i -pe 's/เจ้าค่ะ/ขอรับ/g'     # special-case FIRST (avoids เจ้าขอรับ)
perl -i -pe 's/ค่ะ/ขอรับ/g'
perl -i -pe 's/ครับ/ขอรับ/g'
```
259 lines changed. Verified: no `ขอรับขอรับ`/`เจ้าขอรับ`/`ขอขอรับ`; `ข้าข้า`=61 are PRE-EXISTING stammers (0 added); reads natural (ขออภัยขอรับ/รู้แล้วขอรับ/สวัสดีตอนเช้าขอรับ). **Final game-wide: ครับ=ค่ะ=เธอ=หม่อมฉัน=ดิฉัน = 0.**

### ✅ FULL-FILE QA (2026-07-13, post-v1.1) — CLEAN
name-leak per partition: Menus=2, UI=0, Story01=0, Story02-17=1 — the 3 residuals are FALSE POSITIVES (keyboard keys `[ESC]/[S]/[L]/[A]`, and the translator-credit line `感谢游玩`). Register all 0. ผม/คุณ/ฉัน remain false positives only (เส้นผม/ขอบคุณ-บุญคุณ/ฉันมิตร).

### ⚠️ TOOLING GOTCHA (still true — DO NOT REPEAT)
**Scan Thai with `grep -cP 'คำ' file`.** perl `-CSD -ne '... =~ /คำไทย/'` WITHOUT `use utf8;` gives **FALSE 0**. Byte-level replace `perl -i -pe 's/ก/ข/g'` (no -C) is fine (same UTF-8 bytes both sides). This gotcha is exactly why the old QA missed the Story02-17 register leaks.

### ⬜ REMAINING for v1.1 (all cheap, NO workers)
1. Rebuild the Mod zip → **v1.1** (was `LegendOfMortal-Thai-by-Krirk0137-v1.0.zip` at repo root, gitignored).
2. (optional) git commit + push + tag `v1.1` (user handles GitHub README — fetch+merge, don't clobber).
3. In-game test the opening (Story 01) — user does this.

### FILE / REPO STATE (post-restructure 2026-07-13)
- Dict: `Mod/BepInEx/Translation/th/Text/Translation zh-CN to en.txt` (26MB, LF, no BOM — user confirmed: one giant file, Notepad hangs, use VS Code). Anchor `*.EN-BACKUP`, rolling `*.POST-OPT` (same folder, gitignored).
- Glossary `tools/glossary.tsv` (445). Prompts: `tools/worker_prompt_gold.txt` (story), `worker_prompt_ui.txt`, `worker_prompt_ui_tpl.txt`.
- Git: repo root = `C:\ClaudeCode\LegendOfMortalENG` (Mod/ moved to root; was `LoM_en-main/LoM_en-main/Mod`). Remote `origin` = `github.com/krirk0137/Legend_Of_Mortal_Mod_Thai_by_krirk0137`, branch `main`, tag `v1.0` pushed. User edits README on GitHub web — DON'T clobber; fetch+merge. Release zip built at repo root `LegendOfMortal-Thai-by-Krirk0137-v1.0.zip` (gitignored). `.gitignore` excludes backups/_progress/qwen_test/.claude/*.zip.
- `.EN-BACKUP` line-alignment is intact (line-restore/keydiff still valid).

---


## ✅ Done so far
- **Menu / UI** → Thai.
- **Story 01** (lines 18493–22173, 3,677 entries) → translated in **gold style** and applied. QA passed (0 English leaks; register ~100% ข้า/เจ้า; glossary-consistent).
- **Story 02 — COMPLETE** (orig lines **22174–26057**). First half chunks 00–03 (1,842 entries) + second half chunks 04–08 (2,026 keys) both translated gold-style and applied. Second-half QA clean: 0 English leaks, register wuxia (ข้า 1308 / เจ้า 1240), **22 modern-pronoun เธอ found → all were 3rd-person 她, corrected to นาง**. Residual: ~3 singleton edge-case leaks in the second half (1 line absent from chunks, 1 worker omission `那也是难能可贵了`, 1 tag-wrapped variant `死也不还`) + `[redo verse]` marker — 0.08%, gold-tolerance. Backups `.PRE-STORY02` (before 1st half), `.PRE-STORY02B` (before 2nd half), `.POST-STORY02` (final good state).
- **⚠️ CORRUPTION FOUND & FIXED (this session):** the current game file had **682 lines carpet-overwritten** with a single entry `<size\=44>获得唐门之力</size>=ได้รับพลังแห่งสำนักถัง`. Root cause: the old Story-01 gold TSV (`story01_all_th.tsv`) had **5 malformed rows whose key was the fragment `<size\`** (a TAB had replaced the `=` inside `<size\=24>`). apply_thai's `/^\Q$k\E=/mg` then collapsed to `^<size\=` and overwrote every `<size\=…` line. Fixed by: (1) line-restoring all 682 from `.EN-BACKUP` (line-aligned), (2) re-applying clean TSVs (glossary + Story01 minus the 5 bad rows + Story02 00–08). **Verified: key-diff vs `.EN-BACKUP` = 0**, `<size\=24>` back to 600, `获得唐门之力` = 1 (was 682). `tools/apply_thai.pl` **hardened**: now SKIPS any key ending in `\`. Side effect: 5 Story-01 `<size\=24>` lines (the dropped bad rows) now show English — low priority to re-Thai.
- **Story 03 — COMPLETE** (orig lines **26058–30255**, 4,178 entries). Translated with **~230-line chunks, 3 agents/wave** (new smaller-chunk method). QA: keydiff vs `.EN-BACKUP` = 0, 0 English leaks (all `ฉัน` = `หม่อมฉัน` court pronoun for 本宫/妾身 or `สมานฉันท์` — false positives; 1 `เธอ` → `นาง`). Residual: 3 `──！` punctuation-only lines. Snapshot `.POST-STORY03`. **Guard proved itself: apply_thai SKIPPED 4 fresh malformed `<size\` fragment rows from a worker's output — no corruption; recovered them via `tools/fixsize.pl`.**
- **Story 04 — COMPLETE** (orig lines **30256–34224**, 3,964 entries). Translated with **~460-line chunks (13 total, c04_00–c04_12), 2–3 agents/wave**. Assembled at `_progress/story04/story04_gold.tsv`. QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (11 residual lines were punctuation-only — ellipsis/`!?`/size-tags, matching prior chapters' pattern), 0 `เธอ` found, all `ฉัน`/`ผม` hits were false positives (`ฉันมิตร`/`ฉันสามีภรรยา` idiomatic "as", `หลิวฉัน` name transliteration, `ผม`=hair in ปลงผม/ทรงผม/เส้นผม). **Caught 4 worker key-transcription drifts before applying** (worker altered the CN key slightly when writing output, e.g. dropped/changed a character) — these showed up as 0-hit lines from `apply_thai.pl`, were traced back to the exact source chunk line, hand-corrected in the gold TSV to match the source exactly, then re-applied clean (0 zero-hit lines on re-run). New names added to `tools/glossary.tsv`: 萤儿=อิงเอ๋อร์, 崆峒/崆峒派=คงถง/สำนักคงถง, 上官=ซ่างกวน. Snapshot `.POST-STORY04`.
- **Story 05 — COMPLETE** (orig lines **34225–38312**, 4,085 entries). Translated with **~460-line chunks (9 total, c05_00–c05_08), 2 agents/wave**. **New failure modes caught this chapter (worker_prompt_gold.txt hardened accordingly):** (1) two workers ignored the TAB-delimiter instruction and wrote the source's `=` as the output delimiter instead — caught by the routine tab-count check (0 tabs found), full chunk redone from scratch both times, now works; (2) a worker's output for c05_02 had a fabricated extra `[check]` line not present in source (inflated count 459→460) plus one drifted key (吧→吗) — caught via a proper key-level diff (see `_progress/extract_keys.pl`, handles the `<size\=NN>` embedded-`=` edge case) after the tab-count came back one over; both hand-fixed, re-verified 0 diff; (3) chunk c05_01's gold TSV was missing its final trailing newline, which silently glued its last line to c05_02's first line on `cat`-assembly (total came back 4084 instead of 4085) — caught by the running total not matching, fixed by appending the missing newline. **Lesson: whenever a chunk's tab-count doesn't land exactly on the expected number, do a full sorted key-diff against the source (via `_progress/extract_keys.pl`) rather than assuming it's a one-off — the count alone doesn't say whether it's an extra fabricated line, a dropped line, or a merge.** QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (18 residual punctuation-only lines), 1 `เธอ` found (both instances were 她 3rd-person referring to 小师妹 → corrected to `นาง`), `ฉัน`/`ผม` hits all false positives (`ฉันมิตร`/`หม่อมฉัน`/`ฉันชายหญิง` idiomatic, `ผม`=hair). New names added to `tools/glossary.tsv`: 云裳=หวินฉาง, 颜疆=เหยียนเจียง. Snapshot `.POST-STORY05`.
- **Story 06 — COMPLETE** (orig lines **38313–42362**, 4,044 entries). Translated with **~460-line chunks (9 total, c06_00–c06_08), 2 agents/wave**. Ran a full key-diff on every single chunk this time (not just when the tab-count looked off) — worth it: found 2 more instances of the same "worker drops the `<size\=NN>...</size>` tag wrapper from the key" bug (c06_03, c06_05 — 3 lines total, all hand-restored) plus 1 single-character key drift inside a classical poem line (c06_04: 陇亩→陇争, nonsensical substitution, hand-corrected) that a naive count check would have completely missed since the total still matched. `worker_prompt_gold.txt` and per-chunk prompts now explicitly warn against stripping tag wrappers, plus a trailing-newline reminder (no missing-newline issue actually recurred this chapter — all 9 chunks had one). QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (1 residual punctuation-only line), 1 `เธอ` found (她 3rd-person referring to 师姐 → corrected to `นาง`), `ฉัน`/`ผม` hits all false positives (`ฉันพี่น้อง`/`ฉันทลักษณ์`/`หม่อมฉัน` idiomatic, `ผม`=hair). New name added to `tools/glossary.tsv`: 默铃=โม่หลิง. Snapshot `.POST-STORY06`.
**Lesson reinforced: tab-count matching the expected total is necessary but not sufficient — always run the full sorted key-diff via `_progress/extract_keys.pl` on every chunk, since a 1-for-1 character/tag substitution or drift leaves the count untouched and is otherwise invisible until it silently fails to apply (or worse, mis-applies) downstream.**
- **Story 07 — COMPLETE** (orig lines **42363–46270**, 3,903 entries). Translated with **~460-line chunks (9 total, c07_00–c07_08), 2 agents/wave**. Ran the mandatory full key-diff on every chunk again — found 1 more single-character key drift (c07_01: `轮得到` → `轮到`, dropped `得`), hand-corrected. All other 8 chunks were clean first try. QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (10 residual punctuation-only lines), 0 `เธอ` found, `ฉัน`/`ผม` hits all false positives (`เดียดฉันท์`/`หม่อมฉัน` idiomatic, `ผม`=hair). No new proper names — all of this chapter's characters (袁无宪, 叶云舟, 夏侯兰, 南宫远) were already in `tools/glossary.tsv`. Snapshot `.POST-STORY07`.
- **Story 08 — COMPLETE** (orig lines **46271–50013**, 3,732 entries). Translated with **~460-line chunks (9 total, c08_00–c08_08), 2 agents/wave**. Full key-diff on every chunk found 1 more fabricated non-source line (c08_03: `[redo priority Late August Year 1]`, same class as Story05's `[check]`) — removed. Also confirmed a genuinely pre-existing bare marker line `[redo priority]` (no `=`, source itself) in c08_05, correctly excluded by workers without any fix needed — not every bracketed marker is a fabrication, check the source before assuming. **Two NEW issues caught in the leak/consistency scan (beyond the standard pronoun/English checks), found AFTER the first apply+snapshot — had to re-apply + re-snapshot:** (1) `ครับผม` (a modern Thai male speech particle containing the banned `ผม` pronoun) used twice for generic `是。` acknowledgments — inconsistent with this project's established `ขอรับ` convention for the same source line elsewhere; fixed both to `ขอรับ`. (2) The recurring character 王帮主 (Chief Wang) was transliterated two different ways across chunks in the SAME chapter (`เจ้าพรรควาง` in c08_04, `เจ้าพรรควัง` in c08_05) — neither matched the glossary's already-established `王=หวาง` convention (per `王二壮=หวางเอ้อร์จ้วง`); standardized all 16 occurrences to `เจ้าพรรคหวาง`. **Note:** this same inconsistency (วาง/วัง instead of หวาง) also exists pre-existing in Story 02's applied text (lines ~25000–25300) — out of scope to fix now, flagged as known minor polish below. QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (9 residual punctuation-only lines), 0 `เธอ`, 0 `ฉัน`, `ผม` all false positives after the ครับผม fix. New names added to `tools/glossary.tsv`: 阿浅=อาเชี่ยน, 王=หวาง (standalone surname entry). Snapshot `.POST-STORY08`.
**Lesson: cross-chunk name/term consistency is a DIFFERENT check from the per-line leak/pronoun scan — after assembling all chunks, grep for any character/place name that recurs across multiple chunks and confirm every occurrence uses the identical Thai rendering, not just that each individual line "looks right" in isolation. Do this BEFORE the first apply if possible (cheaper); if caught after, the fix is simply to re-run `apply_thai.pl` on the corrected gold TSV (it re-splices by key) and re-run keydiff + leak scan + re-snapshot.**
- **Story 09 — COMPLETE** (orig lines **50014–53770**, 3,740 entries). Translated with **~460-line chunks (9 total, c09_00–c09_08), 2 agents/wave**. Baked the หวาง/no-modern-particle lessons from Story 08 directly into every chunk's prompt this time — **0 `ครับ`/`ครับผม` leaks and 0 王-surname variant mismatches this chapter** (clean on the first pass, no re-apply needed). Full key-diff on all 9 chunks: all perfect matches, no fabricated lines, no dropped/drifted keys. Confirmed genuine pre-existing source markers (`[redo move]` ×4 in c09_01, `[redo priority]`-style, no `=`) correctly excluded by workers without any fix needed. Cross-chunk consistency check: `小梅`→`เสี่ยวเหมย` consistent (45×), `上官萤`→`ซ่างกวนอิง` consistent (19×) — verified this is legitimately a DIFFERENT name from `萤儿`→`อิงเอ๋อร์` (11×), not an inconsistency: line 57 shows the character self-referring as `萤儿`/`อิงเอ๋อร์`, i.e. a nickname/diminutive for the same person (上官萤), same pattern as 小师妹's other nicknames — both renderings are correct and should NOT be merged. QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (5 residual punctuation-only lines), 1 `เธอ` found (她 3rd-person referring to 小梅 → corrected to `นาง`), `ฉัน`/`ผม` hits all false positives (`เอกฉันท์` idiomatic, `ผม`=hair). New name added to `tools/glossary.tsv`: 小梅=เสี่ยวเหมย (this was flagged as Story 01 residual English months ago — now has a real glossary entry). Snapshot `.POST-STORY09`.
- **Story 10 — COMPLETE** (orig lines **53771–57409**, 3,633 entries). Translated with **~460-line chunks (8 total, c10_00–c10_07), 3 agents/wave** (back to 3 per the concurrency bump). Full key-diff on every chunk caught **4 single-character key drifts across 3 chunks** — all invisible to the count check, all hand-restored to match source exactly: c10_02 (`武功`→`高手`), c10_05 (`好端端`→`好端好` and `余地`→`余度`), c10_07 (`小僧`→`小僆`). **1 API-overload stall** (c10_03 produced 0 output on first launch) — cheaply re-run fresh, own file-existence check caught it (consistent with the ~1-in-27 baseline; 3-workers/wave stays fine). QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (14 residual punctuation-only lines — `…?`/`…!`/`──!`/size-tagged `!!`), 0 `เธอ`, 0 `ฉัน`, `ผม`/`คุณ` all false positives (`ผม`=hair in ปลายผม/โกนผม/เส้นผม/มัดผม/หวีผม; `คุณ`=ขอบคุณ/คุณธรรม/คุณหนู/บุญคุณ/พระคุณ). **Cross-chunk name consistency: 100% clean, ZERO new glossary names needed** — every recurring name was already in `tools/glossary.tsv` and all workers rendered them identically: 福韫→ฝูอวิ้น (20×), 崆峒→คงถง, 玄功→เสวียนกง (16×), 上官→ซ่างกวน, 夏侯→เซี่ยโหว, 唐升→ถังเซิง (3×), 邹博→โจวป๋อ / 邹掌门→เจ้าสำนักโจว, 青城→ชิงเฉิง, 申屠龙→เซินถูหลง. (Note: `邹`(Zōu) and `周`(Zhōu) both transliterate to `โจว`; the recurring surname here is 邹掌门/邹博, NOT 周 — 周 appears only in common words 周遭/周全/周天/周围. And `ถังเจิง` is 唐铮, a distinct name from 唐升, not a drift.) Snapshot `.POST-STORY10`.
- **Story 11 — COMPLETE** (orig lines **57410–60326**, 2,910 entries). Translated with **~460-line chunks (7 total, c11_00–c11_06; c11_06 short at 155), 3 agents/wave**. **Cleanest chapter yet: ZERO key drifts across all 7 chunks** (full key-diff per chunk all empty first try). **1 API-overload stall** (c11_05 produced 0 output on first launch) — cheaply re-run fresh (2nd overload stall in ~15 chunks across Story 10–11, both trivially recovered; 3-workers/wave stays fine). QA: keydiff vs `.EN-BACKUP` = 0, 0 real English leaks (10 residual punctuation-only lines — `？！`/`…!`/`???`/size-tagged `?!`), 0 `เธอ`, `ฉัน` (2, both idiomatic `ความรักฉันพี่น้อง`/`ฉันศิษย์ร่วมสำนัก` = "in the manner of"), `ผม` (4, all =hair: ผมขาว/ผมร่วง/หวีผม), `คุณ` (131, all compounds ขอบคุณ/คุณหนู/คุณธรรม/บุญคุณ/พระคุณ). **Cross-chunk name consistency: 100% clean, ZERO new glossary names** — every recurring name already in `tools/glossary.tsv` and rendered identically: 崆峒→คงถง (74×), 青城→ชิงเฉิง (52×), 上官→ซ่างกวน (47×), 龙→หลง (龙湘=หลงเซียง/龙渊=หลงหยวน), 魏→เว่ย (魏菊=เว่ยจวี๋; 崆峒玄功 sect leader 魏才女=เว่ยไฉหนี่ว์), 南宫→หนานกง (37×), 邹→เจ้าสำนักโจว (34×), 玄功→เสวียนกง (14×), 申屠龙→เซินถูหลง, 唐升→ถังเซิง. (Note: `段` appears 13× but is the common word 一段/段落/手段, NOT the surname 段=ตวน — correctly left untransliterated.) Snapshot `.POST-STORY11`.
- **Story 12 — COMPLETE** (orig lines **60327–65303**, 4,962 entries — the largest chapter). Translated with **~460-line chunks (11 total, c12_00–c12_10), 3 agents/wave (4 waves)**. Full key-diff per chunk caught **2 key drifts in c12_08** (dropped `想` in `持惜花令想找`; final `！`→`。` in `臭弟侠！`) — hand-restored. **1 API-overload stall** (c12_10, re-run). Confirmed 2 genuine pre-existing `[redo]`/`[redo move]` no-`=` source markers (c12_05, c12_06, c12_07) correctly excluded by workers. **TWO NEW failure modes this chapter:**
  1. **⚠️ Embedded-TAB key (NEW):** exactly one source line's KEY contains literal TAB characters — `致 弟：后山。\t\t你姊=...` (a handwritten in-game note with tab spacing). This breaks (a) the `cut -f1` verify (truncates key at first tab → false-positive key-diff) and (b) **`apply_thai.pl` itself** (its `split(/\t/,$l,2)` mis-parses the key, so the line applies `-> 0` and is silently skipped). The worker translated it correctly. Detection: scan the source range for keys containing a tab (`tabkeys.pl` in scratchpad). Fix: after the main apply (which reports exactly 1 expected `-> 0`), run `emb_apply.pl <FILE> <gold.tsv>` (scratchpad) — it re-keys on all-fields-but-last and applies just the embedded-tab line(s). Verified applied (1 hit). **For future chapters: always run the embedded-tab scan; if any key has a tab, expect that many `-> 0` on the main apply and clear them with `emb_apply.pl`.**
  2. **⚠️ `ดิฉัน` modern-pronoun leak (NEW, register):** a female character's classical humble self-reference (妾身/我) was rendered by a worker as `ดิฉัน` (modern polite feminine "I") 11× — a register violation (locked style = `ข้า` for everyone). The plain `ฉัน` grep DID catch it (ดิฉัน contains ฉัน), but it's easy to wave off as a false positive alongside สมานฉันท์/ฉันมิตร. **Lesson: when the `ฉัน` scan fires, expand each hit to the full word — `ดิฉัน`/`ดิชั้น` are real pronoun leaks to fix (→`ข้า`), distinct from the idiomatic `สมานฉันท์`/`เอกฉันท์`/`ฉันมิตร`/`ฉันพี่น้อง` false positives.** Fixed all 11 → `ข้า`.
  Also a genuine **cross-chunk term inconsistency** caught & standardized: 惜花令 (锦香宫's decree-token) rendered 3 ways — `คำสั่งเซี่ยฮวา` (c12_08), `ป้ายเซ็กฮวา` (c12_10, wrong Cantonese-ish translit เซ็ก for 惜 xī), and contextual `บัญชีดำ` "blacklist" (c12_09, the list-sense, left as-is — reads naturally). Standardized the two token-sense uses to **`ป้ายเซี่ยฮวา`**. QA: keydiff vs `.EN-BACKUP` = 0, 0 English leaks (12 residual punctuation-only lines), 0 `เธอ`, `ฉัน` now 4 (all idiomatic), `ผม` 9 (all =hair), `คุณ` 208 (all compounds). New glossary names: 锦香宫=จิ่นเซียงกง, 小竹=เสี่ยวจู๋, 惜花令=ป้ายเซี่ยฮวา, 仙儿=เซียนเอ๋อ, 温夫人=ท่านหญิงเวิน (石郎→พี่สือ, 龙湘→หลงเซียง already consistent; 段 = common word 段落/手段, not surname). Snapshot `.POST-STORY12`.
- **Story 13 — COMPLETE** (orig lines **65304–69363**, 4,053 entries). 9 chunks (c13_00–c13_08), 3 agents/wave. **Zero real key drifts** (per-chunk key-diff only showed the known `银两>=N`/`贡献度>=20` `>=` false positives — extract_keys splits on the `=` inside `>=`; all confirmed matching source). No stalls this chapter. **0 `ดิฉัน` this time** (the register warning baked into the prompt worked) — register fully clean first-pass (0 เธอ, 0 ฉัน entirely, ผม all =hair, คุณ all compounds). QA: keydiff=0, 5 residual punctuation-only lines. **One place-name inconsistency standardized:** 正心堂 (Zhengxin Hall) had a consistent name-core เจิ้งซิน but 3 different 堂-classifiers (หอ ×2 / ตำหนัก ×4 / เจิ้งซินถัง ×3) — unified all 9 to **หอเจิ้งซิน** (หอ=hall; avoids the ถัง/唐 homophone collision) and added to glossary. All other names glossary-consistent (向掌门→เจ้าสำนักเซี่ยง 44×, 龙湘→หลงเซียง 36×, 默铃→โม่หลิง 19×, 南宫深→หนานกงเซิน, 解无尘→เซี่ยอู๋เฉิน, 夺魄幽兰→กล้วยไม้ผีชิงวิญญาณ). Snapshot `.POST-STORY13`.
- **Story 14 — COMPLETE** (orig lines **69364–73098**, 3,726 entries). 9 chunks (c14_00–c14_08), 3 agents/wave. **1 stall** (c14_00 watchdog-killed after 600s no progress, 0 output — re-run fresh). Found **4 real key drifts** across 4 different chunks — the widest spread yet: c14_00 dropped leading `也` from a key start, c14_03 converted a **traditional-character key to simplified** (`小師妹`→`小师妹` etc. — a NEW drift class, worth watching for since this file has both variants as separate keys), c14_04 changed `四师弟`→`四师兄`. All hand-restored, all confirmed via full key-diff. **⚠️ Most serious finding — a live recurrence of the `<size\` malformed-key bug class (the same one that carpet-overwrote 682 lines pre-Story-01):** c14_07's worker did a **naive "replace first `=`" for its TAB delimiter**, and on 7 lines whose value happens to contain a `<size\=NN>` tag *before* the real key/value boundary, it grabbed the WRONG `=` (the one inside the tag) — producing 7 lines like `<size\` + TAB + `50>...</size>=<size\=50>Thai</size>` (a key truncated to the dangling fragment `<size\`, i.e. exactly the pattern `apply_thai.pl`'s hardened guard exists to catch). **`apply_thai.pl`'s existing SKIP-on-trailing-`\` guard would have silently no-op'd all 7** (safe, no corruption — the hardening from the original incident held), but the 7 lines would never have gotten their Thai. Caught by the routine per-chunk key-diff (showed `<size\` as a truncated key vs the source's full tag). Hand-repaired all 7: restored the internal TAB back to `=` at the correct position from the CN key, then re-split the correct CN key / Thai value — and also normalized the Thai value's `<size\=NN>` number back to match the CN key's NN (the worker had drifted 24→20 for readability in 5 of 7; the project-wide convention, confirmed against an already-applied Story-01 entry, is the value keeps the SAME NN as the key). **Lesson for future chunks: any chunk containing `<size\=NN>` tags is now higher-risk for the naive-first-`=`-replacement bug — after the routine key-diff, also grep the gold TSV for a bare `<size\` + TAB pattern (`grep '^<size\\\t'`) as an explicit extra check**, since this failure is a DIFFERENT root cause from the historical wrapper-stripping bug and produces the exact same dangerous key shape. QA: keydiff vs `.EN-BACKUP` = 0, 2 residual punctuation-only lines, 0 `เธอ`, 0 `ดิฉัน`, 1 `ฉัน` (idiomatic สมานฉันท์), ผม/คุณ all false positives. Cross-chunk names 100% consistent, **zero new glossary entries needed** (云裳→หวินฉาง 103×, 云舟→หวินโจว 7×, 青城→ชิงเฉิง 157×, 邹→โจว/โจวป๋อ, 唐芳→ถังฟาง; noted 方少侠 "Young Hero Fang" also renders ฟาง — a legitimate Mandarin homophone with 芳, not a naming inconsistency). Snapshot `.POST-STORY14`.
- **Story 15 — COMPLETE** (orig lines **73099–79231**, 6,123 entries — 2nd-largest chapter). 14 chunks (c15_00–c15_13), 3–5 agents/wave across 5 waves. **The messiest chapter yet — 3 NEW failure modes found, all now guarded against in future prompts:**
  1. **⚠️ Worker used `=` as delimiter for an ENTIRE chunk (not just one line):** c15_03 and (on first attempt) c15_05 both output every line as `CHINESE=ENGLISH<TAB>Thai` — i.e. copied the full source line (including English) as field 1 instead of just the CN key. Silent because tab-count was still correct (1 tab/line) — only the full sorted key-diff caught it (100% mismatch). **Recovered without a full redo**: wrote `repair_cnen_key.pl` (scratchpad) — splits on first TAB, then re-applies the `extract_keys.pl`-style negative-lookbehind regex to field 1 to recover the true CN key. Worked perfectly on c15_03 (460/460 recovered). c15_05's redo also failed with a different variant of the same `=`-instead-of-TAB bug and had to be fully redone a 2nd time (successfully, after adding an extremely explicit repeated warning to the prompt).
  2. **⚠️ Worker included bare no-`=` marker lines as if they were real translated entries** (3 separate instances: c15_07 line 154 "？！", c15_08 line 218 "？！") — inflating the count and adding a fake untranslated entry. Caught by key-diff extra-line detection; confirmed via 1:1 context-line comparison against source that the flagged line was genuinely a bare marker (no `=`) before deleting.
  3. **Single-character/word key drifts** (c15_04: dropped leading `也`, and separately a raw Thai character glyph got inserted mid-key via encoding corruption; c15_06: `嘴`→`厖` typo + an included bare `玖` marker; c15_07: `你`→`我` swap at a key's start; c15_05: a 15-times-repeated `说不说` phrase under-copied by one repetition, 53→50 chars) — all caught by full key-diff, all hand-restored.
  Also: **1 mid-stream API stall** (c15_06 first attempt, near-zero tokens, re-run) and **2 watchdog stalls** (c15_11, c15_12, both 600s-no-progress, 0 output, cheaply re-run). This chapter alone had far more redo/repair work than any prior chapter — attributable partly to session length (this was the 4th chapter translated in one very long continuous session, which likely contributed to the account's 5-hour usage limit being hit mid-chapter; work paused and safely resumed after reset with all completed chunk files persisted on disk). QA: keydiff vs `.EN-BACKUP` = 0, 10 residual punctuation-only lines, 0 `เธอ`, 0 `ดิฉัน`, ฉัน/ผม/คุณ all idiomatic/compound false positives. Cross-chunk names 100% consistent (云裳→หวินฉาง, 段智秀→ตวนจื้อซิ่ว, 南宫→หนานกง, 邹→โจว, 默铃→โม่หลิง, 唐芳→ถังฟาง, 铁冠道人→นักพรตมงกุฎเหล็ก, 郁竹→อวี้จู๋, 叶云舟→เย่หวินโจว — all already glossary-established). New glossary entry: 松阉→ซงเอียน (a one-off vulgar joke name). Snapshot `.POST-STORY15`.
- **Name glossary** `tools/glossary.tsv` (440 entries: wuxia terms + places + full character roster) built and **applied game-wide** — character/speaker names show Thai everywhere.
- **Font** `OverrideFont=Tahoma` (+`ResizeUILineSpacingScale=1.2`), `EnableConsole=True`. Confirmed readable in-game.

## 🔒 Locked style decisions
- Translate **Chinese-primary**, English value = reference only.
- **Wuxia register**: ข้า / เจ้า / ท่าน / ข้าน้อย. Never ฉัน/เธอ/คุณ/ผม.
- **Proper names → Thai transliteration** (สำนักถัง, ถังปู้อี, ยุทธภพ) per `tools/glossary.tsv`.
- Engine = **Sonnet**. (User's self-hosted Qwen3-30B-A3B was tested and rejected: garbled Thai.)

- **Story 16 — COMPLETE** (orig lines **79232–80122, 887 entries**). **⚠️ NEW failure mode this chapter (the reason it was NOT done as the usual 2×460 chunks):** Story 16's dialogue is unusually long/literary → a full 460-line chunk (429 entries) **blew the 64k single-response output-token ceiling** when the worker wrote its whole gold file in one Write (`API Error: Claude's response exceeded the 64000 output token maximum`, 0 output produced). Separately, the sibling 458-entry chunk's agent **stalled dead at launch** (0-byte transcript unchanged for 2h) and a re-run of one 228-line half **also stalled at launch**. **Fix that worked: split down to ~110–230-line sub-chunks** — done as **5 sub-chunks** (c16_00_00_00=112, c16_00_00_01=116, c16_00_01=230, c16_01_00=216, c16_01_01=213). The ~110-line halves finished in ~10 min each with no stalls. **Detection recap:** a stalled-at-launch agent = its `tasks/<id>.output` transcript stays **0 bytes** and its mtime doesn't advance (vs a working agent whose transcript grows); `TaskStop` it and re-run (optionally smaller). QA all clean: full key-diff vs source = 0 (every one of 887 keys exact — zero drifts/fabrications/stripped-wrappers across all 5 sub-chunks), 0 `<size\`+TAB fragments (both c16_00 halves had `<size\=NN>` tags, handled right — value mirrors the SOURCE English value's NN, e.g. key `<size\=24>`→value `<size\=20>`), register fully clean first-pass (0 เธอ/ดิฉัน/หม่อมฉัน/ครับ/ค่ะ; the lone ฉัน=`ฉันทามติ` consensus, ผม=hair, คุณ=compounds คุณชาย/ขอบคุณ/คุณธรรม/คุณหนู, all FPs), 0 real English/Latin leaks (only the `size` tag keyword + literal `\n`), apply had **0 `-> 0` misses** (2236 total substitutions), keydiff vs `.EN-BACKUP` = **0**, leakscan = 1 punctuation-only residual (`……？！!`, gold-tolerance). Cross-chunk names 100% consistent vs glossary (青城→ชิงเฉิง, 邹/邹掌门→โจว/เจ้าสำนักโจว, 云裳→หวินฉาง, 叶云舟→เย่หวินโจว, 上官→ซ่างกวน, 南宫→หนานกง, 赵活→จ้าวหัว, 无相祖师→ปรมาจารย์ไร้รูป, 铁冠道人→นักพรตมงกุฎเหล็ก). **3 new glossary names** (found the exact CN — note the homophone traps): 赵逵=จ้าวขุย (the traitor "Zhao Kui" — 逵 not 魁), 眉山=เหมยซาน (Meishan, real Sichuan place — 眉 not 梅), 风雨山=เขาเฟิงอวี่ (Fengyu Mtn). Snapshot `.POST-STORY16`.

## ✅✅✅✅ WHOLE FILE TRANSLATED — Story 01–17 + ALL optional UI/descriptions (2026-07-13) ✅✅✅✅
- **Optional block COMPLETE** (orig lines **437–18492**, ~16,100 English entries). Rolling snapshot **`.POST-OPT`** (keydiff vs `.EN-BACKUP` = 0; only 16 punctuation-only residuals `……！`/`──！`/`？？？` remain in the whole block). Two halves:
  - **Pre-UI (437–6135, ~4,161 lines):** 24 sections (System, Stat, all item/equipment/book/special/misc, PlayerInfo, Position, Facility, CombatInfo, BattleSkill, Talent01, CombatEffects, LectureHall, StoryMessage, BattleTalk, EnemyTeam, Flags, **CharIntro** cast-bios, **DuelTalk** combat banter) → Sonnet workers 3/wave with `tools/worker_prompt_ui.txt` (UI/description rules + locked stat/combat term dict). Handled: 1 embedded-TAB key (攻擊45, via `emb_apply.pl`), `<color=#..>` keys (apply-safe, verify uses source-prefix match), a `承传↔传承` transposition drift, and verbose bios/books split to ~20-line sub-chunks for the 64k ceiling. `_progress/optional/verify_opt.sh` = per-chunk verifier; `PROGRESS.md` = full section status.
  - **UI Changes (6137–18492, 11,972 lines):** heavily templated (quest counters, stat deltas, item stat breakdowns) → **dedupe-431-unique-templates + expand**: mask digit-runs to a `§` sentinel, translate each of 431 unique VALUE-templates once (`tools/worker_prompt_ui_tpl.txt`, pinned term dict for cross-worker consistency), then `_progress/optional/ui_expand.pl` re-inserts each concrete line's own numbers into the §-slots (expanded=11972, miss=0, slotbad=0; apply 0 `-> 0`). **Gotcha baked into the tooling:** the `ui_*.pl` scripts lack `use utf8;` so the `§` literal is actually the 2-char string `Â§` (C2 A7 as latin1) — consistent everywhere so lookups match, but count/substitute the 2-char sentinel `"\x{c2}\x{a7}"`, never `tr/§//`.
- Glossary `tools/glossary.tsv` now **445 entries**. Prompts added: `tools/worker_prompt_ui.txt`, `tools/worker_prompt_ui_tpl.txt`.
- **Nothing else is English.** The whole `Translation zh-CN to en.txt` (menus + Story 01–17 + all UI/item/skill/description content) is Thai.

## ✅✅✅ STORY 17 COMPLETE — ENTIRE STORY MODE (Story 01–17) TRANSLATED (2026-07-12) ✅✅✅
- **Story 17 — COMPLETE** (orig lines **80123–85814, 5016 entries**). Finished as ~99 leaf work-units (~230-line chunks recursively split to ≤35 KB eighths for the 64k output-token ceiling on dense late-chapter prose). Final 16 units this session via Sonnet general-purpose workers, 3/wave, single-write/no-bash prompt — all PASS first try, **0 drifts, 0 stalls, 0 corruption**. Assembled `story17_gold.tsv` = **5016 lines** (integrity gate). Register: 7 `เธอ`→`นาง` (all 3rd-person 她 for 乐娘子/乐屏/上官萤); 0 ดิฉัน/หม่อมฉัน/ครับ/ค่ะ; ฉัน (4, idiomatic ฉันสามีภรรยา/ฉันเพื่อน/ฉันนั้น) + ผม (all =hair) false positives. **Name-consistency lesson: the glossary was already authoritative — 南宫浅→หนานกงเชี่ยน (87×), 向无忧→เซี่ยงอู๋โหยว (52×), 画中仙→นางฟ้าในภาพวาด — all matched glossary + already-applied Stories 01–16; the `_newnames_notes.tsv` "STD" forms (เฉียน/โยว) were an OUTDATED earlier guess. Always reconcile against `tools/glossary.tsv` + the applied game file, not stale session notes.** Only fix needed: 5× `ฮว่าจงเซียน`→`นางฟ้าในภาพวาด`. 78 duplicate keys confirmed source-inherent (branching dialogue), not an assembly artifact. Applied on `.POST-STORY16`: 0 `-> 0` misses (6937 total substitutions, key repeats game-wide), **keydiff vs `.EN-BACKUP` = 0**, leakscan 80123–85814 = 1 punctuation-only residual, 0 English words in values, no BOM/CRLF. **5 new glossary names** (全真派=สำนักฉวนเจิน, 尹道长=นักพรตอิ๋น, 洞庭=ต้งถิง, 向掌门=เจ้าสำนักเซี่ยง, 夔州=ขุยโจว), applied game-wide (全真派 hit 2 standalone labels), keydiff still 0. Snapshot **`.POST-STORY17`** (= current game file, the whole story mode is now Thai).
- **What remains (optional, never in story scope):** `UI Changes` (lines 6137–18492) + non-story item/skill descriptions. Everything narrative (Story 01–17) is done.

## 🗄️ (historical) IN PROGRESS — Story 17 (last chapter) — now COMPLETE, see above.
Story 01–16 all done (base = current game file = `.POST-STORY16`, keydiff vs `.EN-BACKUP` = 0). Story 17 = orig lines **80123–85814, 5016 entries, 25 chunks (~230 each)**, no embedded-TAB keys. All chunks split on disk at `_progress/story17/chunks/`; gold outputs at `_progress/story17/out/`. **Full per-chunk manifest (count + final-key checkpoint + done/pending) is at `_progress/story17/RESUME.md` — read that first.**
- **DONE + verified (key-diff=0, tab-count OK):** c17_00 (228), c17_01 (230), c17_02 (163), c17_03 (114). c17_01's key-diff shows the known `>=` false positive (`解锁条件：心相>=50`) — that's fine, the key is correct.
- **PENDING:** c17_04 … c17_24 (21 chunks, ~4281 entries).
- **⚠️ WHY FRESH SESSION:** this session translated Story 16 then Story 17 waves 1–2 back-to-back and got very long; by wave 2 **agent spawns started stalling at launch** (0-byte transcript, dead — c17_04 & c17_05 each stalled 2×, even a SOLO relaunch of c17_04 stalled). This is the documented "long session → stall cluster" mode; the fix is a fresh session (see [[chunking-and-concurrency]] / [[lom-thai-translation]]). Nothing is corrupted — the game file is still clean `.POST-STORY16`; only the pending gold TSVs aren't written yet.
**Resume recipe (fresh session):** for each PENDING chunk, spawn a Sonnet general-purpose agent whose prompt says: *do it yourself (no delegate); read `tools/worker_prompt_gold.txt` and follow it exactly with {CHUNK_PATH}=`_progress/story17/chunks/cNN.txt`, {OUT_PATH}=`_progress/story17/out/cNN_gold.tsv`, {COUNT}=<from manifest>, {CHECKPOINT}=<from manifest>; read `tools/glossary.tsv` first.* Run 3/wave. Per chunk verify: tab-count==count AND full key-diff (`_progress/extract_keys.pl`, ignore `>=`/`<size\=` false positives), no BOM, trailing `\n`, `grep -cP '^<size\\\t'`=0 — or just `verify_halves.sh <base>` which does all of it. Then assemble c17_00…c17_24 IN ORDER → `story17_gold.tsv` (expect **5016** lines), register scan (เธอ→นาง / expand ฉัน→ดิฉัน-check / ผม=hair / คุณ=compounds), cross-chunk name consistency vs glossary, `apply_thai.pl` on the game file, keydiff=0 + leakscan on 80123–85814, snapshot `.POST-STORY17`. **That completes the whole game (Story 01–17).**
**⚠️ 2026-07-11 session — stall-detection CORRECTION + 3 new failure modes (all now baked into `worker_prompt_gold.txt`):**
- **The "0-byte transcript = stalled" heuristic is DEAD in the current harness build**: `tasks/<id>.output` stays 0 bytes for the agent's entire life — even for an agent that completes successfully (proven with a 5-second test agent). Do NOT health-check via transcript size; 6 healthy agents were needlessly killed that way. **Ground truth = the gold TSV appearing/growing in `out/` + the harness completion notification.** With single-write workers the file only appears at the very end, so rely on completion notifications and be patient (~15-25+ min); only treat as hung after ~45+ min with no completion.
- **Worker abandons Write tool for bash heredocs (NEW, c17_08):** after ~2h of slow incremental writing, the 230-line c17_08 worker switched to bash heredocs, failed on the large content, and overwrote its 148 finished lines with test garbage (`SIMPLETEST`/`LINE2`). Killed + redone as 2×115 halves. Prompt now forbids bash-writing the output file.
- **English-word key leak (NEW, c17_09):** worker replaced `申屠龙强行运功` mid-key with `forcibly循环` (a word from the English reference). Only 1 line; caught by the mandatory full key-diff + a `cut -f1 | grep -cP '[A-Za-z]'` latin-in-key scan (a good quick extra check). Hand-repaired byte-level. **Beware: `perl -CSD -pe 's/CN.../CN.../'` silently no-ops on files when the pattern has non-ASCII (encoding mismatch) — use a byte-level `perl -i -pe` (no -C) for such repairs and verify the diff actually cleared.**
- **Incremental-write slowness (root cause of the above):** 230-entry chunks made workers write ~4 lines/min across 30-40 tool calls (~90 min, ~395k tokens/chunk). Fix: **~115-line halves + explicit "translate everything first, ONE single Write call" instruction** (now in the template). Chunks c17_08 + c17_11..c17_24 are pre-split on disk as `chunks/c17_NN_h00/h01.txt` (28+2 halves); per-half count+checkpoint manifest at `_progress/story17/_halves_manifest.txt`; batch verifier `_progress/story17/verify_halves.sh`.
**General rule for late narrative chapters:** ~230-line chunks (split to ~115 on error), NOT 460 — long literary chunks hit the 64k single-response output-token ceiling (proven in Story 16).

**Chunk-size default (still applies to Story 10–17):** use **~460-line chunks**, NOT 230 — see [[chunking-and-concurrency]] memory for the token-efficiency math (measured: 230-line chunks cost ~375–415 tokens/entry vs ~285/entry for 460-line, because each agent spawn pays a ~50k-token fixed overhead regardless of chunk size — fewer, larger chunks pay that overhead fewer times). The tab-count verify-and-rerun step (already standard practice) is the real anti-truncation safety net, not chunk size.
**Failure modes seen so far (all now guarded against in `tools/worker_prompt_gold.txt` — keep an eye out anyway):**
- Agent's background API stream stalls mid-response before writing any output file → re-launch that chunk fresh (own file-existence check catches this).
- Worker slightly mistypes/drops a character in the CN key when writing its output TSV, including inside long classical-poem lines → invisible in the gold TSV itself; either shows up as `apply_thai.pl`'s `-> 0` (zero-hit) line once applied, or (worse) is silently missed entirely if the count still matches — **this is why a full key-diff per chunk, not just a count check, is mandatory** (see below).
- Worker strips the `<size\=NN>...</size>` tag wrapper off a key, keeping only the inner text — happened 3 times across Story 06 alone. Same detection/fix as above: full key-diff, hand-restore the wrapper.
- Worker uses the source's `=` as its output delimiter instead of a TAB → shows up as 0 in the `grep -cP '\t'` count; redo the chunk.
- Worker fabricates an extra non-source line (e.g. a stray `[check]` marker) → inflates the tab-count past the expected total; only a full key-diff (not just the count) reliably finds it.
- A chunk's gold TSV is missing its final trailing newline → silently glues its last line to the next chunk's first line during `cat`-assembly, making the assembled total 1 short. **Always check the assembled total against the sum of chunk counts, and check every chunk file ends in `\n` before concatenating** (`tail -c1 file | od -An -c`).
- **Cross-chunk name/term consistency** — a recurring proper name translated two different ways by two different workers across chunks of the SAME chapter (Story 08: `王帮主` as both `เจ้าพรรควาง` and `เจ้าพรรควัง`, neither matching the glossary's already-established `王=หวาง`). This is invisible to the per-line leak/pronoun scan (each individual line "looks right" in isolation) and to the key-diff (keys are fine, only values differ). **Separate check needed:** after assembling all chunks, grep the assembled gold TSV for any name/place that recurs and confirm every occurrence renders identically. Also watch for a bare non-`=` marker line (e.g. `[redo priority]`) that's genuinely PRE-EXISTING in the source itself (not a fabrication) — check the raw source line before assuming any bracketed marker is a worker hallucination.

**⚠️ Mandatory verification step, upgraded after Story 06: run a full sorted key-diff on EVERY chunk, not just when the tab-count looks off.** A 1-character drift or a stripped tag wrapper leaves the total count untouched — the count check alone would have missed 4 of the corruptions found in Story 06. Procedure per chunk:
```
perl _progress/extract_keys.pl < chunks/cNN_XX.txt | sort > /tmp/src_sorted.txt
cut -f1 out/cNN_XX_gold.tsv | sort > /tmp/gold_sorted.txt
diff /tmp/src_sorted.txt /tmp/gold_sorted.txt   # must be empty (or only the known >= false-positive, see below)
```
`_progress/extract_keys.pl` splits on `=` with a negative lookbehind for `\` so it doesn't break on `<size\=NN>` tags — but it does NOT handle keys containing a literal unescaped `>=` (e.g. `解锁条件：嘴力>=50`), which shows as a false-positive diff line; verify any such hit by hand against the raw source line before treating it as real.

## 📋 Remaining work — **NONE (all 17 stories DONE).** Optional-only: `UI Changes` (6137–18492) + item/skill descriptions.
| Story | Lines (start–end) | Story | Lines (start–end) |
|---|---|---|---|
| ~~02~~ | ~~22174–26057~~ ✅ DONE | ~~10~~ | ~~53771–57409~~ ✅ DONE |
| ~~03~~ | ~~26058–30255~~ ✅ DONE | ~~11~~ | ~~57410–60326~~ ✅ DONE |
| ~~04~~ | ~~30256–34224~~ ✅ DONE | ~~12~~ | ~~60327–65303~~ ✅ DONE |
| ~~05~~ | ~~34225–38312~~ ✅ DONE | ~~13~~ | ~~65304–69363~~ ✅ DONE |
| ~~06~~ | ~~38313–42362~~ ✅ DONE | ~~14~~ | ~~69364–73098~~ ✅ DONE |
| ~~15~~ | ~~73099–79231~~ ✅ DONE | | |
| ~~07~~ | ~~42363–46270~~ ✅ DONE | ~~16~~ | ~~79232–80122~~ ✅ DONE |
| ~~08~~ | ~~46271–50013~~ ✅ DONE | 17 | 80123–85814 (EOF) ← **NEXT (last)** |
| ~~09~~ | ~~50014–53770~~ ✅ DONE | ~~17~~ | ~~80123–85814~~ ✅ DONE (game complete) |

Also still English (optional, later): `UI Changes` (6137–18492), and non-story item/skill descriptions.

## 🔁 Pipeline to translate one Story (repeat per chapter)
Paths use the session scratchpad `$SP` and the game file `$FILE`
(`.../Mod/BepInEx/Translation/th/Text/Translation zh-CN to en.txt`).

1. **Extract + chunk** the story's line range (from the pristine `$FILE.EN-BACKUP` for clean CN=EN):
   ```
   sed -n '<START>,<END>p' "$FILE.EN-BACKUP" > "$SP/storyNN_src.txt"
   split -l 460 -d --additional-suffix=.txt "$SP/storyNN_src.txt" "$SP/chunks/cNN_"
   ```
   **Chunk size ~460 lines** (reversed from an earlier 230-line default — see [[chunking-and-concurrency]] memory for the token-efficiency math). The tab-count + full key-diff verify step (below) is the real anti-truncation safety net, not chunk size.
2. **Translate** each chunk: spawn one Sonnet Agent per chunk using
   `tools/worker_prompt_gold.txt` (substitute {CHUNK_PATH} / {OUT_PATH}).
   **Keep the anti-delegate first line** or agents duplicate/skip work.
   Run **3 agents at a time** in small waves (bumped from 2 on 2026-07-06 — see [[chunking-and-concurrency]] memory; watch for stalls clustering, drop back to 2 if they do) — finishes each Part sooner, easier to fix, gentler on the 5-hour limit.
   **After each chunk: verify (a) output tab-line count == source content-entry count** (`grep -cP '^[^/].*=' chunk` vs `grep -cP '\t' out`) **and (b) a full sorted key-diff against the source** (see the procedure a few sections up — mandatory even when the count matches, since single-character drifts and stripped tag wrappers don't change the count). The truncation bug is SILENT and the worker may even report a wrong "complete" count — re-run any shortfall (re-prompt with the exact count + paste the final source key as a checkpoint to reach).
3. **Assemble + fix register + apply** (apply on top of the previous chapter's verified `.POST` — which IS this chapter's safe base; no separate PRE needed):
   ```
   cat "$SP/out/cNN_*_gold.tsv" | grep -P "\t" > "$SP/storyNN_gold.tsv"
   # fix modern pronoun เธอ: inspect each line, 她(3rd-person)→นาง, 你(2nd)→เจ้า
   perl -i -pe 's/เธอ/นาง/g' "$SP/storyNN_gold.tsv"   # ONLY after confirming all เธอ are 3rd-person
   perl tools/apply_thai.pl "$FILE" "$SP/storyNN_gold.tsv"   # SKIPs malformed `\`-ending keys automatically
   ```
4. **QA — verify the APPLIED FILE, not just the gold TSV** (this is the lesson from the 682-line corruption; a clean gold TSV can still leave the file corrupted). All must pass:
   ```
   perl tools/keydiff.pl "$FILE.EN-BACKUP" "$FILE"     # MUST be 0 mismatches (no carpet-overwrite)
   G="$SP/storyNN_gold.tsv"
   for w in Tang Sect Sichuan jianghu; do echo "$w: $(grep -o "$w" "$G" | wc -l)"; done   # want 0
   for w in ฉัน เธอ ผม; do echo "$w: $(grep -oP "$w" "$G" | wc -l)"; done                 # want ~0
   # no-Thai-value scan on this chapter's line range (spot real leaks) — see session scratchpad snippet
   ```
   (Note: `คุณ` is fine — ขอบคุณ/คุณธรรม/คุณชาย, not the pronoun. Verify context if unsure.)
5. New proper names found this chapter → add to `tools/glossary.tsv` for consistency.
6. **✅ Snapshot ONLY after step 4 passes** — a backup of an unverified file just preserves bugs:
   ```
   cp "$FILE" "$FILE.POST-STORYNN"   # known-good checkpoint AND base for the next chapter
   ```
   Keep `.EN-BACKUP` (pristine anchor) + the latest one or two `.POST-STORYNN` uncompressed; older ones can be `gzip`-ed (16 MB → ~3 MB). The stale corrupted `.PRE-*` snapshots can be deleted.

## ⚠️ 5-hour usage limit
**Measured per-chunk cost (Story 02, ~461 entries/chunk, Sonnet worker):** 108k–144k tokens each,
avg **~130k tokens/chunk (~285/entry)**; ~19 min wall-clock. A full ~9-chunk story ≈ **~1.2M tokens ≈ ~35 pts**
of the 5-hour limit; **1 chunk ≈ ~4 pts**. 8 parallel Sonnet agents spike the 5-hour limit hard (one chapter
≈ 35%→70%). Full remaining ≈ ~18M tokens. **Translate 1–2 chapters per session, let the window recover,
use 4 parallel agents.** Starting a FRESH session also cuts per-turn cost (drops this long conversation's context).

## 🧹 Known minor polish (optional)
- 3 stray modern pronouns leaked in Story 01 (ฉัน×1, ผม×2) — cosmetic.
- Stacked Thai tone marks (ตั้ง) clip slightly in-game; long labels truncate to button width — user accepts.
- **Story 01 residual English (~72 lines, pre-existing):** mostly roster labels / short name+punctuation the dialogue-gold + glossary don't key exactly (`小梅=Xiaomei`, `四师兄？`, `阿活！`, `唐门弟子(嵩山口味)`). Plus the 5 `<size\=24>` lines from the dropped malformed rows. Low priority.
- **QA lesson:** the old "0 English leaks" check scanned the *gold TSV*, not the *applied file* — it missed both the 682 wrong-Thai corruption (Thai, but wrong) and these uncovered labels. Prefer verifying the applied file: `keydiff.pl` (keys vs `.EN-BACKUP`, must be 0) + a no-Thai-value scan per line-range. Both scripts saved in the session scratchpad.
- **Story 02 residual name inconsistency (found while fixing Story 08):** the recurring character 王帮主 (Chief Wang) is transliterated as `เจ้าพรรควาง`/`เจ้าพรรควัง` in Story 02's applied text (lines ~25000–25300) instead of the glossary-standard `เจ้าพรรคหวาง` (per `王=หวาง`). Same character reappears in Story 08, already fixed there. Low priority — cosmetic naming inconsistency only, not an English leak or corruption; would need Story 02's original gold TSV (or the applied range) hand-corrected and re-applied if ever revisited.

## 💾 Backups of the dictionary
Lean policy (see also memory `backup-discipline`): keep **`.EN-BACKUP`** (pristine, line-aligned recovery anchor — never touch) + the latest one or two **`.POST-STORYNN`** (snapshot taken only AFTER verify passes; doubles as the next chapter's base). Current on disk uncompressed: `.EN-BACKUP`, `.POST-STORY15`, `.POST-STORY16`, **`.POST-STORY17` (FINAL good state: Story 01–17 ALL applied, key-diff vs EN-BACKUP = 0 — the whole game story mode is Thai)**. `.POST-STORY02`–`.POST-STORY14` all gzip'd/superseded; `.POST-STORY15` can now be gzip'd too. Revert by copying one back.

## 🛠️ Tooling (all in `tools/`)
`apply_thai.pl` (splice Thai by CN key; **skips malformed `\`-ending keys**) · `keydiff.pl` (key integrity vs EN-BACKUP, must be 0) · `repair.pl` (line-restore corrupted keys from EN-BACKUP) · `fixsize.pl` (reconstruct `<size\`+TAB fragment rows a worker sometimes emits) · `tabkeys.pl` (scan a source range for KEYS containing literal TABs — run per chapter) · `emb_apply.pl` (apply embedded-TAB-key gold lines that `apply_thai.pl` can't; clears the expected `-> 0`) · `leakscan.pl` (no-Thai-value scan over a piped line range) · `repair_cnen_key.pl <bad_gold.tsv> > <fixed.tsv>` (NEW, Story 15 — repairs a gold TSV where a worker output `CN=EN<TAB>Thai` — i.e. included the English in field 1 — instead of `CN<TAB>Thai`, by re-extracting the true CN key from field 1 with the same negative-lookbehind rule as `extract_keys.pl`; only usable when the corruption is systemic/whole-chunk, confirm first with `cut -f1 gold.tsv | grep -c '='` ≈ line count) · `glossary.tsv` · `worker_prompt_gold.txt`. (`_progress/extract_keys.pl` = per-chunk key extractor for the key-diff.)
